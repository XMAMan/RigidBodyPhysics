<UserControl x:Class="KeyFrameEditorControl.Controls.Slider.SliderControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KeyFrameEditorControl.Controls.Slider"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Orientation="Horizontal">
        <StackPanel Orientation="Horizontal">
            <CheckBox IsChecked="{Binding IsAnimated}" Margin="5 5"/>
            <Label Content="{Binding Index}"/>
            <Slider Minimum="{Binding MinValue}" Maximum="{Binding MaxValue}" Value="{Binding Value}" Width="100" Margin="0 3"/>
        </StackPanel>
    </StackPanel>
</UserControl>
